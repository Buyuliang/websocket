## Install software environment dependencies

```bash
sudo apt update
sudo apt -y install libboost-system-dev libboost-thread-dev libsqlite3-dev libjsoncpp-dev libpthread-stubs0-dev libwebsocketpp-dev
```
